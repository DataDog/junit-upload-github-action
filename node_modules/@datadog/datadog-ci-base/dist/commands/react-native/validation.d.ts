import type { RNSourcemap } from './interfaces';
export declare class InvalidPayload extends Error {
    reason: string;
    constructor(reason: string, message?: string);
}
export declare const validatePayload: (sourcemap: RNSourcemap) => void;
