import { BaseCommand } from '@datadog/datadog-ci-base';
export declare class ReactNativeCodepushCommand extends BaseCommand {
    static paths: string[][];
    static usage: import("clipanion").Usage;
    private appCenterAppName;
    private appCenterDeployment;
    /**
     * There should not be multiple uploads with the same version in the case
     * of codepush, so we can go with a default of "1".
     */
    private buildVersion;
    private bundle;
    private configPath;
    private disableGit;
    private dryRun;
    private maxConcurrency;
    private platform?;
    private projectPath;
    private removeSourcesContent;
    private repositoryURL;
    private service;
    private sourcemap;
    private releaseVersion?;
    private fips;
    private fipsIgnoreError;
    private config;
    execute(): Promise<number>;
    private getReleaseVersionFromCodepushHistory;
}
