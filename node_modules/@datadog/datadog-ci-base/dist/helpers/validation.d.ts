import * as t from 'typanion';
export declare const checkFile: (path: string) => {
    empty: boolean;
    exists: boolean;
};
/**
 * Check if a string is a valid Datadog site.
 *
 * If the environment variable `DD_CI_BYPASS_SITE_VALIDATION` is
 * set, then it will return `true`.
 *
 * @param site an optional string.
 * @returns a boolean indicating if the provided site is valid a Datadog site.
 */
export declare const isValidDatadogSite: (site?: string) => boolean;
export declare const checkAPIKeyOverride: (environmentAPIKey: string | undefined, configFileAPIKey: string | undefined, stdout: {
    write: (message: string) => void;
}) => void;
export declare const isInteger: () => t.StrictValidator<unknown, number>;
